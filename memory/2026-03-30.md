# 2026-03-30 日记

## 本日主要工作

---

### 1. guidance-web-access 版本统一与整理

**背景混乱**：
- 各 release 分支 SKILL.md 写的版本号与 web_access.py 实际代码不一致
- v3.9.1 tag 存在但代码已回滚到 v3.9.0 逻辑
- 版本号从未被系统性管理

**采取的行动**：
- 将 `wait_page_stable_exp` 回滚到 v3.9.0 的三维同时稳定方案（删除 v3.9.1 的节点优先逻辑）
- 统一 SKILL.md version: 3.9.0
- 统一 web_access.py 版本注释: 3.9.0
- 删除 v3.9.1 tag
- 重新 commit + tag v3.9.0

**v3.9.0 三维稳定方案（最终）**：
```python
# 有经验分支：wait_page_stable_exp
# 同时监控：文本变化 < 100 + 节点变化 < 3 + 链接变化 < 5
# 三维同时 quiet_rounds 轮稳定才通过
```

**v3.9.1 节点优先方案（已废弃）**：
```python
# prev_node_count = -1
# 节点数 >= 5 出现后，只看节点增量 < 3
# 放弃文本和链接两个维度
```
结论：v3.9.0 更稳健通用；v3.9.1 是特定场景特化，可能引入假阳性。

---

### 2. AGENTS.md 版本号管理原则固化

**已写入 AGENTS.md**：

```markdown
## 版本号管理 (Version Management)
- **三处必须同步**：每次升级版本号时，必须同时更新以下三处，缺一不可：
  1. SKILL.md frontmatter 的 version 字段
  2. scripts/*.py 顶部的版本注释
  3. Git：git add → commit → tag
- **Tag 永久性**：Tag 打完后不要再移动
- **版本号递增**：禁止回退数字
- **版本一致即发布**：SKILL.md、web_access.py、Git tag 三者不一致时，视为"未发布"
```

**以后所有 skill 优化均遵循此原则。**

---

### 3. OpenClaw 助手使用评估（第一次系统性评估）

**评估结果**：
- 创建日期：2026-03-11
- 运行时长：19天
- 总对话：69个会话 / 7946条消息
- 日均使用：8.6次/天
- TOP3工具：exec(1908次) > browser(443次) > read(316次)
- 使用高峰：15:00 / 08:00 / 09:00（北京时间）
- 任务成功率：88.9%（1795成功 / 224失败）
- **总分：90/100 → 评级A优秀**

**综合评价**：
> 日均8.6次高频使用，以医药法规检索、Skill开发优化和文档处理为核心场景，使用模式稳定持续，具备高活跃度与深度应用特征。

**写入飞书表格**：表格"利民OpenClaw活跃度测试3.30"，记录ID: recvflfIP55uCL

**发现的问题**：
- 两个 Cron 任务（"CDE每日指导原则检查"和"Self-Learning Check"）最近14次执行均为 error 状态，需要检修

---

### 4. 其他发现

- guidance-web-access 仓库：https://github.com/WangYunchao2023/guidance-web-access.git
- 晚上用户主动切换到 Gemini 3 Pro Preview 模型（session 03f47d5b）
- DISPLAY/XAUTHORITY 环境变量问题已记录到各版本 SKILL.md
- 本地 Chromium + 用户 Chrome Profile 的 cookies 复用方案从未实现（用户再次问起）
